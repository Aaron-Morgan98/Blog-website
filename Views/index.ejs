<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" 
    rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" 
    crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
    
</head>
<body>
    <%- include("partials/header.ejs") %>

    <div class="px-4 py-5 my-5 text-center">
        <h1 class="display-5 fw-bold text-body-emphasis">Welcome to my blog!</h1>
        <div class="col-lg-6 mx-auto">
          <p class="lead mb-4">A space to share stories, insights and experiances. 
         <br>This digital space is a collective canvas for creativity, knowledge, and collaboration.</p>
          <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
            <button type="button" class="btn btn-primary btn-lg px-4 gap-3" 
            onclick="window.location.href='<%= redirectUrl %>'">Create post</button>
          </div>
        </div>
      </div>
    <hr>
 



      <br><br>
    <div class="latestPosts">
        <h2> Latest Posts:</h2>
       
        <% if (posts && posts.length > 0) { %>
            <ul>
                <% posts.forEach((post,index) => { %>
                    <li>
                        <h3><%= post.title %></h3>
                        <h5>Author: <%= post.author %></h5>
                        <p><%- post.body.replace(/\n/g, '<br>') %></p>
                        
                        <a href="/edit/<%= index %>"><button class="btn btn-primary rounded-pill px-3" type="button">Edit</button></a>
                        <form action="/delete/<%= index %>" method="POST">
                            <button class="btn btn-danger rounded-pill px-3" type="submit">Delete</button>
                        </form> 
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <p>No posts available... Yet!</p>
        <% } %>
        </div>
            
    </div>


    <%- include("partials/footer.ejs") %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>